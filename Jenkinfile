pipeline {
    agent any

    stages {
        stage('Build') {
            steps {
                echo 'Building..'
                sh "curl https://mise.run | sh"
                sh "curl -fsSL https://get.pnpm.io/install.sh | env PNPM_VERSION=10.1.0 sh -"
                sh "source /var/lib/jenkins/.bashrc"
                sh "mise use node@latest"
                sh "node --version"
            }
        }
        stage('Test') {
            steps {
                echo 'Testing..'
            }
        }
        stage('Deploy') {
            steps {
                echo 'Deploying....'
            }
        }
    }
}
